<form>
  <div class="form-container">
    <div class="input-fields">
      <label for="input-amount">
        <Input id="input-amount"
               placeholder="Enter a number!"
               @type="text"
               @value={{this.currentInputValue}}
          {{on "input" this.inputValue}}
        />
      </label>
      <label for="select-currency">
        <select id="select-currency"  {{on "change" this.selectCurrency}}>
          {{#each this.responseValue as |itemResponse|}}
            <option value={{itemResponse.ccy}}>{{itemResponse.ccy}}</option>
          {{/each}}
        </select>
      </label>
    </div>
    <div>
      <button id="button-convert"
              type="submit"
        {{on "click" this.convert}}
      >Convert
      </button>
    </div>
    <div class="result-block">
      <h2>Conversion result</h2>
      <div>{{this.result}}</div>
      <h2>History</h2>
      {{#each this.finallyHistoryList as |item|}}
        <div>{{item}}</div>
      {{/each}}
    </div>
  </div>
</form>